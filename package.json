{
  "name": "logistic-escrow",
  "version": "1.0.0",
  "description": "Decentralized logistics escrow platform (BMIS2003) — agreements, milestone payments, refund disputes, and carrier reputation tokenisation.",
  "private": true,
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --network development",
    "migrate:reset": "truffle migrate --reset --network development",
    "test": "truffle test",
    "console": "truffle console --network development"
  },
  "devDependencies": {
    "truffle": "^5.11.5"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "dotenv": "^17.4.2",
    "ethers": "^6.17.0",
    "truffle-plugin-verify": "^0.6.7"
  }
}
